const Footer = () => (
  <footer className="mt-16 border-t border-gray-200 py-8 text-center text-sm text-gray-500 dark:border-gray-800">
    © {new Date().getFullYear()} LearnHub — built with the MERN stack.
  </footer>
);

export default Footer;
